import { AllCryptoProps } from "@/Utils/types";
import React from "react";

const CryptoCards = ({ name, value, image, quantity }: AllCryptoProps) => {
	return <div>CryptoCards</div>;
};

export default CryptoCards;
